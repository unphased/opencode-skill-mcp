---
name: test-skill
description: A test skill for integration testing
---

# Test Skill

This is a test skill with an echo MCP server.

When asked to echo something, load this skill first, then use the `echo` tool via `skill_mcp`.
When asked to add numbers, load this skill first, then use the `add` tool via `skill_mcp`.
